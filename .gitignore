# Binaries / local tooling
bin/
dist/
tmp/

# Env & secrets
.env
.env.*
!.env.example

# PocketBase local data — la DB real vive en el volumen Docker `helpdesk_pb_data`,
# nunca como carpeta del proyecto. Estos paths solo evitan commits accidentales.
pb_data/
docker/pocketbase/pb_data/

# IDE / OS
.DS_Store
.idea/
.vscode/
*.swp

# Go
vendor/
