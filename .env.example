# Copy to .env and adjust. Never commit real secrets.

PB_ADMIN_EMAIL=admin@helpdesk.local
PB_ADMIN_PASSWORD=helpdesk-admin-change-me
PB_PORT=8090
# Host port for the Helpdesk UI (container still listens on 8080 inside).
API_PORT=3000
